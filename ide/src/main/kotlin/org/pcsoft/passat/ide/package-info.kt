/**
 * Standalone Passat IDE assembly (scaffold).
 *
 * This package will host the entry points and product configuration for the standalone Passat IDE
 * once standalone-IDE assembly is implemented (roadmap phase 5). For now it only reserves the
 * module namespace [org.pcsoft.passat.ide].
 */
package org.pcsoft.passat.ide
