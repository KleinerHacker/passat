/**
 * Passat plugin root package.
 *
 * Hosts everything that needs project context and therefore does NOT belong in the reusable
 * [org.pcsoft.passat.language] core: the Pascal project/module type, the FPC toolchain/SDK and
 * target language version, the dependency list, run configurations, the build/compile integration
 * and the debugger.
 */
package org.pcsoft.passat.plugin
